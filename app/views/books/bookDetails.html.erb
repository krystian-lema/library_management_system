<h1>Books</h1>
<table>
  <tr>
    <th>Title</th>
    <th>Author</th>
    <th>Edition</th>
    <th>Publication date</th>
    <th>ISBN</th>
    <th>Signature</th>
    <th>library</th>
    <th>Status</th>
    <th>Action</th>
  </tr>
 
  <% @books.each do |book| %>
    <tr>
      <td><%= book.title %></td>
      <td><%= book.author %></td>
      <td><%= book.edition %></td>
      <td><%= book.publication_date %></td>
      <td><%= book.ISBN %></td>
      <td><%= book.signature %></td>
      <td><%= book.library.name %></td>
      <td>Any informarmations!</td>
      <td><%= link_to 'Show', '/details' %>
      	<%= link_to 'Borrow', '/borrow' %>
      </td>
    </tr>
  <% end %>
</table>